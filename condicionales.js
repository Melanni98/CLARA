// For => Este ciclo tiene la declaracion de la variable, la condicion y el incremento en una sola linea
for (let i = 0 ; i < 7; i++) {
    console.log ("for"+ i)
}

let numeros = [10, 20, 30, 50, 80]
for (let i = 0; i < numeros.length; i ++) {
    console.log (numeros [i]);
}